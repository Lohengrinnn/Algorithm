1. in union() of union-find
   x = find(u)
   y = find(v)
   if x != y:
      union(x, y) -----not not not------ union(u, v)

2. divide and conquer

   def partition(l, r):
      small = l - 1 # not -1
